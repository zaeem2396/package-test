# Copy these lines into your local `.env` (never commit `.env`).
# Replace placeholders with values from Orkes → Access Control → Applications → access key.

CONDUCTOR_SERVER_URL=https://your-cluster.orkescloud.com/api
CONDUCTOR_AUTH_KEY=your-key-id
CONDUCTOR_AUTH_SECRET=your-key-secret
